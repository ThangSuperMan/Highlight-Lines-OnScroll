import React from 'react'
import s from '../styles/banner.module.css'

const Banner = () => {
	return (
		<div
			className={s.container}
		>
			<span className={s.title}>700px</span>
		</div>

	)
}

export default Banner
